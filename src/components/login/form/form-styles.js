const styles = theme => ({
    margin: {
        margin: theme.spacing.unit,
    },
    main: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '100vh'
    },
    button: {
        marginTop: '20px'
    }
});

export default styles;