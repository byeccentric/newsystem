const styles = theme => ({
    progress: {
        margin: theme.spacing.unit * 2,
    },
    wrapper: {
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: 'translate(-50%, -50%)'
    }
});

export default styles;